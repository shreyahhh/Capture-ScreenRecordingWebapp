# Backup of your current .env
# Server Configuration
PORT=8080

# Database Configuration
# ORIGINAL CONNECTION STRING (if the new one doesn't work):
# MONGO_URI=mongodb+srv://shreya:esuu9opcZatSyEkb@capture1.5xzqimc.mongodb.net/capture-screenrecorder?retryWrites=true&w=majority&appName=capture1

# NEW TEST CONNECTION STRING (use after creating backenduser):
MONGO_URI=mongodb+srv://backenduser:Backend123@capture1.5xzqimc.mongodb.net/capture-screenrecorder?retryWrites=true&w=majority&appName=capture1

# Frontend Configuration
FRONTEND_URL=http://localhost:3000
